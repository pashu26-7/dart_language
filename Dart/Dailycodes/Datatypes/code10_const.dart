void main(){
  const int x = 10;
  print(x);
  x = 50;
  print(x);
}
/* here also the error will form because if we make any varabile constant then after if changing the vale of that varabile it will give the error
 because overall the value of that object has become constant soo we cannot change it .....*/  